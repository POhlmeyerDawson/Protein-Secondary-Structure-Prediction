import hashlib

for fold in range(5):
    path = f"checkpoints/final_model_fold_{fold}.pth"
    with open(path, "rb") as f:
        checksum = hashlib.md5(f.read()).hexdigest()
    print(f"Fold {fold} checksum: {checksum}")
